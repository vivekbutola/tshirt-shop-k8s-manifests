apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: tshirt-shop
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  selector:
    app: api
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
